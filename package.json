{
  "name": "den-root",
  "private": true,
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "vp check",
    "test": "echo 'no tests in Sub A — Sub C will replace this with: vp run test:tier2 && vp run test:tier3a'"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.0"
}
