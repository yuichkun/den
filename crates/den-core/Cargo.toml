[package]
name = "den-core"
version = "0.0.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
# (empty for now — Sub B adds no_std-friendly deps; Sub D pulls in fundsp optionally)
